ex:GAPANAINID a ex:Parametro ;
    ex:textoExtracto "GAPANAINID Número de entrada analógica conectada al sensor. Valores posibles (1): Si GAPANAINTYPE=Remoto CAN, de 1 a 60. Valores posibles (2): Si GAPANAINTYPE=Regulador, de 101 a 132 o de 201 a 232. Valores posibles (3): Si GAPANAINTYPE=Captación, número de nodo del regulador. Valores posibles (4): Si GAPANAINTYPE=EtherCAT, de 1 a 1024. Valor por defecto: Ninguna (no hay control del gap). Variable asociada: (V.)MPG.GAPANAINID El sensor puede estar conectado a una salida analógica de los módulos remotos CAN o de un regulador Sercos. En ambos casos, este parámetro identifica la salida analógica que se está usando. Se recomienda utilizar una entrada del regulador. Entrada analógica de los módulos remotos CAN. En este caso, este parámetro indica el número de la salida analógica que se utiliza para la consigna. Los módulos de salidas analógicas se numeran según el orden lógico de los grupos remotos. Si hay más de un módulo de salidas analógicas dentro de cada grupo, el orden es de izquierda a derecha. Gestión de la consigna analógica desde los reguladores Sercos. En este caso, este parámetro indica cuál es el número de la salida analógica y el regulador a utilizar. El parámetro se define mediante un número de tres dígitos; el primer dígito indica el número de la salida analógica a utilizar (1 ó 2) y los dos dígitos siguientes indican la dirección lógica del regulador (de 1 a 32). La dirección lógica la establece el conmutador rotativo "address". Por ejemplo, si el parámetro se define con el valor ꞏ107ꞏ, indica que se utiliza la entrada analógica ꞏ1ꞏ del regulador cuya dirección lógica es ꞏ7ꞏ. Entrada de captación de un regulador. Esta prestación requiere una versión de regulador v9.10 o posterior." ;
    rdfs:label "GAPANAINID" .

ex:SensorGAPANAINID a ex:Sensor ;
    ex:textoExtracto "El sensor puede estar conectado a una salida analógica de los módulos remotos CAN o de un regulador Sercos." .

ex:ModuloRemotoCAN a ex:ComponenteElectrico ;
    ex:textoExtracto "Entrada analógica de los módulos remotos CAN. En este caso, este parámetro indica el número de la salida analógica que se utiliza para la consigna. Los módulos de salidas analógicas se numeran según el orden lógico de los grupos remotos. Si hay más de un módulo de salidas analógicas dentro de cada grupo, el orden es de izquierda a derecha." .

ex:ReguladorSercos a ex:ComponenteElectrico ;
    ex:textoExtracto "Gestión de la consigna analógica desde los reguladores Sercos. En este caso, este parámetro indica cuál es el número de la salida analógica y el regulador a utilizar. El parámetro se define mediante un número de tres dígitos; el primer dígito indica el número de la salida analógica a utilizar (1 ó 2) y los dos dígitos siguientes indican la dirección lógica del regulador (de 1 a 32). La dirección lógica la establece el conmutador rotativo "address". Por ejemplo, si el parámetro se define con el valor ꞏ107ꞏ, indica que se utiliza la entrada analógica ꞏ1ꞏ del regulador cuya dirección lógica es ꞏ7ꞏ. Entrada de captación de un regulador. Esta prestación requiere una versión de regulador v9.10 o posterior." .

ex:ConmutadorAddress a ex:ComponenteElectrico ;
    ex:textoExtracto "La dirección lógica la establece el conmutador rotativo "address"." .