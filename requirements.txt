# ==========================================
# SemanticRAG - Gemelo Digital Cognitivo
# Dependencias del Entorno
# ==========================================

# --- Core y Entorno ---
python-dotenv==1.0.1      # Carga de variables de entorno desde .env

# --- Motor LLM ---
mistralai==0.1.8          # SDK oficial de Mistral (o usa 'requests' si haces llamadas crudas)
requests==2.31.0          # Llamadas HTTP (GraphDB API, endpoints REST)
aiohttp==3.9.3            # Peticiones asíncronas (útil para micro-batching)

# --- Web Semántica y Grafo (Knowledge Graph) ---
rdflib==7.0.0             # Parseo, manipulación y serialización de grafos RDF (T-Box/A-Box)
SPARQLWrapper==2.0.0      # (Opcional) Wrapper especializado para consultas SPARQL remotas

# --- ETL, Telemetría y Análisis de Datos ---
pandas==2.2.1             # Manipulación de DataFrames, despivote (melt) de CSVs de Fagor
numpy==1.26.4             # Operaciones vectoriales subyacentes

# --- Bases de Datos (TimescaleDB / PostgreSQL) ---
psycopg2-binary==2.9.9    # Driver de PostgreSQL para insertar telemetría en TimescaleDB
# SQLAlchemy==2.0.29      # (Descomentar si prefieres usar un ORM en lugar de queries puras)

# --- Utilidades y Evaluación ---
pydantic==2.6.4           # Validación estricta de estructuras JSON (Golden Sets, Contratos)
tqdm==4.66.2              # Barras de progreso en consola (muy útil para bucles de extracción)